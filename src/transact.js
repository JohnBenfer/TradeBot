const axios = require('axios').default;
module.exports = {
  sell: (ticker, price) => {
    // console.log("selling at " + price);
  },
  buy: (ticker, price) => {
    // console.log("buying at " + price);
  }
};
